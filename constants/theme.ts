/** Must match the key used in index.html inline script (anti-flash). */
export const THEME_KEY = 'gitavox-theme';

export const BG_LIGHT = '#f1f5f9';
export const BG_DARK = '#030712';

export const PARTICLE_COLOR = '#3b82f6';

/** Number of particles in the 3D scene. */
export const PARTICLE_COUNT = 3000;
/** Min/max distance from origin for particle sphere. */
export const PARTICLE_DISTANCE_MIN = 5;
export const PARTICLE_DISTANCE_MAX = 10;
/** Rotation speed divisors (lower = faster). */
export const PARTICLE_ROTATION_X_DIV = 10;
export const PARTICLE_ROTATION_Y_DIV = 15;
export const PARTICLE_SIZE = 0.05;
export const PARTICLE_OPACITY = 0.4;
/** Camera distance from origin. */
export const SCENE_CAMERA_Z = 8;
/** Initial group rotation (radians). */
export const SCENE_GROUP_ROTATION_Z = Math.PI / 4;
